# ===== App data — NEVER commit (contains bookings, invoices, hashed passwords) =====
# Only the root-level data/ folder is ignored — demo/data/ stays tracked.
/data/

# Real location names override — keeps real building addresses out of public repo.
# Locally and on the production server this file holds DATA.locations with real names;
# without it the app falls back to the fictional baseline in static/index.html.
/static/locations.local.js

# Backups created by demo/start.sh during a demo session
/data.real-backup/
/static/locations.local.js.demo-backup
demo/.index.html.real

*.db
*.sqlite

# ===== Python venvs =====
BM-Venv/
.venv-preview/
venv/
.venv/
env/
ENV/

# ===== Python cache & build =====
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# ===== OS / Editors =====
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# ===== Claude / Cursor / agent dirs =====
.claude/
.cursor/
.aider*

# ===== Logs & temporary =====
*.log
*.tmp
*.bak

# ===== Local env =====
.env
.env.local
.env.*.local
